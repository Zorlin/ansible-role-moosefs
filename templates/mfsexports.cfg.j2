# {{ ansible_managed }}
# Configuration template for the MooseFS exports.
# All of the default MooseFS exports are used if not defined here, so this will be nearly empty in most cases.

# Allow everything but "meta".
*			/	rw,alldirs,admin,maproot=0:0

# Allow "meta".
*			.	rw
